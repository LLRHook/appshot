# Appshot Studio application

See the [repository README](../README.md) for setup, editing, export, architecture, and validation.

```bash
npm ci
npm run dev
```

The native Canvas export pipeline works without a server-side renderer or Chrome debugging flags.
